kind: pipeline
type: docker
name: sync-tifinity-io

steps:
- name: hugo build
  image: jojomi/hugo
  commands:
  - echo hello drone
  - ls && pwd
  - hugo --baseUrl="http://tifinity.github.io/"
  when:
    branch:
    - master

trigger:
  event:
  - push